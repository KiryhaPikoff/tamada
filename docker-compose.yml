version: '3.6'

services:
  postgresql:
    container_name: postgresql
    image: postgres:11
    restart: always
    volumes:
      - /data/postgres:/var/lib/postgresql/data
    ports:
      - 50035:5432

  tamada-service:
    image:
    container_name: tamada-service
    restart: always
    ports:
      - 50040:50040